# 작업 상태

## 2026-09-13 — Feature Selection 시행착오와 후속 후보 기록

- `troubleshooting.md`에 VIF 축소 후 오차 증가, 가격 수준과 수익률의 구분, 예측 기간 확장과 비중첩 시작점 민감도를 정리했다. phase shift·허위회귀가 확정됐다는 표현은 쓰지 않고, 관찰 결과와 해석을 구분했다.
- `architecture.md`에 30거래일 예측·안티오키아 강수 편차의 20일 시차를 우선 후보로, 14거래일 예측·COT 변화의 5일 시차를 비교 후보로 남겼다. 표본 수·관계 방향·정상성 결과와 검증 조건을 기록했다. 30일의 KPSS 기각도 유지했으며 최적 기간이나 최종 피처를 확정한 것은 아니다.
- 검증: 저장된 notebook의 표·출력과 문서 수치를 대조하고 링크·Git diff를 확인했다. 이번에는 문서만 수정했다. notebook·데이터·환경·기존 미커밋 코드 변경을 보존했고, 새 모델 학습이나 후속 구현은 진행하지 않았다.

## 2026-09-13 — 180·360거래일 예측 기간 진단

- `03`의 예측 기간에 180·360거래일을 추가했다. 입력 시차는 0·5·20·60·120거래일, 분석과 정답은 2016~2021년으로 유지했다. 510개 시차상관·102개 분포시차 회귀를 계산하고, FDR도 102개 공동검정 전체에 다시 적용했다.
- 180일은 알타 모지아나 기온 편차·시차 60일(r=0.418, 1,118행), 360일은 우일라 기온 편차·시차 0일(r=0.593, 938행)의 절대 Pearson이 가장 컸다. 360일 금리 변화·시차 60일도 r=-0.526이었다. 변수와 시차가 기간마다 달라 장기로 갈수록 영향이 커졌다고 쓰지 않았다.
- 180·360일의 비중첩 표본은 시작점별 6~7개·2~3개다. 3개 미만이거나 입력이 상수인 경우 상관을 계산하지 않았다. ADF p=0.411·0.802, KPSS는 모두 p<0.01이며, 360일 환율의 비중첩 상관은 -0.943~0.987로 부호가 바뀌었다. 긴 기간은 탐색 결과로 남겼다.
- 검증: Python 3.12.14 새 커널에서 81셀(코드 46셀) 전체 실행. 기존 5일 모델 예측·지표는 허용오차 내에서 같고, 기존 4개 기간의 상관·회귀계수·보정 전 p값도 유지됐다. 미래 원자료 변경 불변성, 거래일·정답 경계, 합성 타깃, HAC, 공통 행, 비중첩 표본과 102개 FDR을 확인했다. 셀 오류·저장된 경고가 없고 새 한글 히트맵 2개도 확인했다.
- 후속 모델 비교는 같은 예측 날짜·과거 이력·재학습 규칙으로 맞춘다. 과거 수정 전 값이 없고 2024~2025년 Test는 이미 본 구간이라는 점을 유지한다. 데이터·환경·다른 미커밋 파일은 변경하지 않았다.

## 2026-09-13 — Train EDA 리팩터링과 중장기 시차 진단

- `03` 앞부분의 분포·정규성·정상성·계절성·상관·VIF·회귀를 2016~2021년 Train 기준으로 정리했다. Validation R²와 기존 5거래일 모델 실험은 뒤로 모으고, 기상 웜업 사례와 플랫폼별 한글 폰트는 유지했다.
- 추가 입력 시차 0·5·20·60·120거래일, 예측 기간 7·14·30·120거래일로 340개 시차상관과 68개 분포시차 회귀를 계산했다. 회귀 하나에는 가격 통제변수 4개와 외생변수 하나의 시차 5개만 넣었다. 공통 행 수는 기간 순서대로 1,290·1,283·1,266·1,178개이며, 모든 정답이 2021년 안에서 끝난다.
- 최대 절대 Pearson 상관은 7·14일 타깃에서 COT 비율의 20일 변화·추가 시차 5일(r=-0.175·-0.291), 30·120일에서는 안티오키아 30일 강수 편차·추가 시차 20일(r=-0.429·-0.487)이었다. Train에서 찾은 탐색 후보이며 피처를 채택하거나 새 예측 모델을 비교한 결과는 아니다.
- 120일 타깃은 연속 654거래일 표본에서 ADF p=0.206, KPSS p<0.01이었다. 30일도 두 검정의 판단이 갈렸다. 120일 비중첩 표본은 시작점별 9~11개이고, 17개 변수 중 9개만 세 시작점에서 전체 표본과 부호가 같았다. HAC·FDR p값만으로 장기 관계를 확정하지 않는다고 적었다.
- 검증: Python 3.12 새 커널에서 81셀(코드 46셀) 전체 실행. 기존 분할·5일 예측값·지표·선택 결과는 허용오차(rtol=1e-10, atol=1e-12) 내에서 유지됐다. 2022년 이후 원자료의 모든 수치에 큰 값을 더해도 Train 입력·월별 기준·시차상관·회귀 결과가 같았다. 합성 시차·누락 거래일·HAC 대조·추정 불가 입력·비중첩 경계를 확인하고 저장된 한글 히트맵 4개도 확인했다.
- 다음 모델 비교에서는 예측 날짜·과거 이력·재학습 규칙을 맞춘다. 과거 수정 전 값이 없고 2024~2025년 Test는 이미 본 기간이라는 점을 유지한다. 데이터·환경·다른 미커밋 변경은 건드리지 않았다.

## 2026-09-13 — Python 3.12·PyCaret 4 전환과 EDA 확장

- 외부 `coffee-price-prediction` 환경을 CPython 3.12.14로 구성하고 PyCaret 4.0.0a8을 설치했다. `timeseries` extra와 호환되는 NumPy 2.4.6·pandas 2.3.3·scikit-learn 1.7.2를 사용한다. requirements는 직접 쓰는 패키지 중심으로 정리하고 커널·환경 안내를 맞췄다.
- `03`에 학습 기간 원자료 히스토그램·박스플롯, VIF와 타깃 상관을 함께 본 피처 선택, 로그 변동성·월별 기온 편차·변화량 파생 실험을 추가했다. 전체 21개 중 16개를 남겨 최대 VIF는 32.58 → 11.00이 됐다. 전체 변수를 함께 계산한 값이라 앞의 기상 묶음 VIF와 다르다.
- 검증 RMSE는 전체 0.047154, 선택 0.047441, 선택+파생 0.048274로 전체 피처를 유지했다. 테스트는 각각 0.054674·0.054911·0.054663이며, 테스트의 소폭 개선으로 선택을 바꾸지 않았다.
- PyCaret은 외생변수 없이 로그 종가로 가격 유지·과거 평균·추세 연장·단순 지수평활·ARIMA(1,1,0)를 비교했다. 가격을 채우지 않으려고 2018-12-06부터 연속 구간을 사용했다. 2022~2023년 5거래일 간격 검증 100건에서 가격 유지가 RMSE 0.045156으로 가장 낮았다. 기존 테스트 498건에서는 RMSE 0.054442로 기존 Naive와 같았다.
- 설치된 4.0.0a8의 내장 CV가 5% 분위수 예측을 평가하는 것을 재현했다. 패키지는 수정하지 않고 `create_model(cross_validation=False)`로 생성한 파이프라인에 검증 창과 점예측을 직접 적용했다. 실행 로그 파일은 남기지 않는다.
- 검증: Python 3.12 새 커널에서 `03` 63셀·`01` 11셀 전체 실행, 기존 테스트 19개 통과. 분할·예측 시점·선택 기준·31개 트리·가격 유지 수익률 0을 확인했다. 저장된 한글 차트도 확인했으며 `uv pip check`는 104개 패키지 호환, requirements 설치 미리보기는 변경 없음이었다. 데이터 재수집은 하지 않았다.

## 2026-09-13 — 피처 그룹별 CatBoost 비교

- `03` 하단에 가격 4개 → 매크로 포함 8개 → COT 포함 9개 → 기상 2곳 포함 13개를 전체 21개와 비교하는 섹션을 추가했다. 모두 depth=4·학습률 0.03·31개 트리로 고정하고, Naive를 포함한 검증·테스트 Markdown 표와 한글 막대그래프를 남겼다.
- Train 기준 기상 묶음의 최대 VIF는 19.33에서 2.27로 낮아졌다. 하지만 기상을 줄인 구성 4의 RMSE는 전체 피처보다 검증에서 1.25%, 테스트에서 0.73% 커졌다. 기상이 없는 구성 3에 남긴 기상 4개를 더하면 RMSE가 각각 2.66%, 1.04% 줄었다.
- 검증 RMSE로는 전체 21개를 선택했다. 테스트에서는 가격 4개가 RMSE 0.054201·방향성 정확도 52.61%로 가장 좋았지만 선택을 바꾸지 않았다. 선택한 전체 모델은 검증·테스트 모두 Naive보다 RMSE가 컸다.
- 외부 가상환경의 새 Jupyter 커널에서 50셀(코드 25셀)을 전체 실행했다. 다섯 구성의 동일한 분할·결측·타깃 경계, 모델 10개의 트리 수, 기존 전체 모델 예측 재현, 표·차트 지표와 한글 표시를 확인했다. 최종 실행의 셀 오류·경고는 없으며 데이터·의존성은 바꾸지 않았다.

## 2026-09-13 — 정규성·정상성·기상 공선성 진단

- `03`에 개발 표본 1,923개의 왜도·첨도·JB 검정과 Q–Q plot을 추가했다. 미래 5일 수익률은 초과첨도 1.479, JB 근사 p=5.78e-50이다. 20일 수익률은 p=0.372로 정규성을 기각하지 못했다. 표본 수와 겹치는 수익률 구간 때문에 p값을 근사로 해석한다고 적었다.
- 정상성은 누락 거래일을 이어 붙이지 않고 2018-12-13~2023-12-21의 연속 1,265거래일에 ADF·KPSS를 적용했다. 종가 ADF p=0.555, 과거 5일 수익률 p=1.88e-9, 미래 5일 수익률 p=1.25e-9이다. KPSS p값은 종가 <0.01, 두 수익률 >0.10이며 검정 범위와 가정을 함께 설명했다.
- 기상 12개 피처의 상관 히트맵, Pearson |r|≥0.8인 조합 7개, VIF를 추가했다. 술 지 미나스–세라도 기온 상관은 0.930, 세라도 기온 VIF는 18.50이다. 피처 선택과 모델 설정은 바꾸지 않았다.
- 외부 가상환경에서 40셀(코드 20셀)을 전체 실행했다. 새 차트 2개 한글 표시, JB 통계량·상관 조합·연속 거래일 검사, 기존 모델 지표 동일을 확인했다. 새 의존성이나 수집 파일은 추가하지 않았다.

## 2026-09-13 — 기상 버퍼와 EDA 보완

- 기상 6개 파일에 2015-12-02~12-31, 2026-01-01~01-30을 추가 수집했다. 파일당 3,713행이며 기존 3,653행의 값·자료형 보존, 날짜 연속성·중복·결측을 확인했다. 다른 소스의 수집 기간은 유지한다.
- `03`에서 중앙값 대치를 제거하고, 초기 29일 NaN을 실제 기온으로 재현했다. 박스플롯·월별 기상 패턴·Pearson/Spearman·종가/미래 수익률 회귀선·HAC/FDR 검정과 검증 R²를 추가했다. 분석은 2016~2023년 개발 구간 기준이다.
- 외부 uv 환경에서 31셀(코드 17셀)을 전체 실행했다. 기상 피처 결측·대치 0개, 학습 1,422행·검증 496행·재학습 1,923행·테스트 498행이다. CatBoost는 depth=4·31 trees, 테스트 RMSE 0.054674로 Naive 0.054442보다 컸다. 중요도 상위는 우일라 기온(25.3), 20일 수익률(11.4), BRL(9.6)이다.
- 검증: 수집 진입점의 기상 버퍼/타 소스 기간 분리, 과거 방향 집계·As-of Join·학습 경계 확인, 차트 12개 한글 표시 확인, `uv pip check` 통과. statsmodels와 필요한 의존성을 설치하고 requirements에 반영했다. 초기 결측 Jupyter 출력 이미지를 `troubleshooting.md`에 남겼다.

## 2026-09-13 — 외부 uv 가상환경 안내 정리

- `AGENTS.md`·`Context.md`의 환경 안내를 프로젝트 외부의 `$HOME/.virtualenvs/coffee-price-prediction`으로 맞추고 활성화·uv 패키지 관리 명령을 명시했다.
- `.env`의 `COFFEE_VENV`·`UV_PROJECT_ENVIRONMENT`는 `python-dotenv`에서도 경로가 확장되도록 `$HOME`을 `${HOME}`으로 바꿨다. 다른 설정은 유지했다.
- 검증: uv 생성 정보·CPython 3.14.7, 문서의 셸 활성화 명령, 두 dotenv 경로가 같은 외부 환경을 가리키는지 확인했다. `git diff --check` 통과. 가상환경 재생성·패키지 변경은 없으며 `.env`는 Git 제외 상태다.

## 2026-09-13 — notebook 시각화 한국어 표시

- `01`·`03` 첫 코드 셀에 운영체제별 폰트 분기를 추가했다. Windows/Mac/Linux는 각각 Malgun Gothic/AppleGothic/NanumGothic을 쓰며 Linux/Colab에는 설치 명령을 주석으로 안내한다. Seaborn에도 선택한 폰트를 전달한다. 변경 후 Mac의 새 커널에서 각각 11셀·19셀을 전체 실행했고 글꼴 경고는 없었다. Windows·Linux에서 직접 실행한 것은 아니다.
- 활성 notebook `01`·`03`의 표 항목명, 산지·피처명, 그래프 제목·축·범례를 한국어로 바꿨다. 설치된 한글 폰트를 선택하며 내부 계산용 컬럼은 유지한다.
- 기존 `.venv` 새 커널에서 각각 10셀·18셀을 전체 실행했다. 차트 3개를 확인했고 한글 글꼴 경고는 없었다. 모델 지표는 동일하다. `old_code/`의 학부 원본은 유지했다.

## 2026-09-13 — EDA·베이스라인

- `03_eda_and_baseline_models.ipynb` 전체 17셀을 기존 `.venv`의 새 커널에서 실행했다. 로컬 소켓 권한으로 실행했으며, 표 출력 셀의 괄호 오류 수정 후 전체 재실행을 통과했다.
- Parquet 12개를 공개 지연 가정으로 As-of Join하고 피처 21개를 만들었다. ICE 공지와 달력을 대조해 휴장일 6행은 모델 입력에서 제외하고, 누락 거래일 3개는 빈칸으로 유지했다. 원본 데이터는 수정하지 않았다.
- 2016~2021년 학습 1,422행, 2022~2023년 검증 496행. 경계에 걸친 5일 뒤 정답을 제외했다. Ridge alpha=100, CatBoost depth=4·31 trees를 고른 뒤 2016~2023년 1,923행으로 재학습했다.
- 테스트 498건의 RMSE는 Naive 0.054442, Ridge 0.057085, CatBoost 0.054631. CatBoost 중요도 상위는 Huila 30일 평균 기온(25.6), 20거래일 수익률(11.5), BRL 환율(9.8)이다. 모델 설정에 테스트 구간은 사용하지 않았다.
- CatBoost 1.2.10과 pandas_market_calendars 5.4.0 및 필요한 의존성을 기존 환경에 추가했다. 과거 발표시각·수정 전 값 부재와 소스별 지연 가정은 notebook에 명시했다. 다음은 FastAPI·차트 화면.
- 검증: 30달력일 기상 집계·FRED 휴일 ffill·COT 발표 지연·5거래일 타깃·학습 경계를 확인했다. 차트 2개를 확인했고 `pip check`, `git diff --check`도 통과했다.

## 2026-09-13 — 디렉터리 정리

- 설계는 `architecture.md`, Yahoo 조사는 `troubleshooting.md`로 통합했다. ADR 4개·reports·references·일회성 tools·중복 수집본·과거 실행 notebook 사본을 제거했다. 이전 준비 문서와 긴 컬럼 사전은 소스 YAML·설계 노트로 정리했다.
- `data/raw/pilot_probe/`에 원응답 16개를 보관한다. FRED·NASA·CFTC는 2026-09-12 08:30 UTC 전체 수집본, Yahoo는 08:40 UTC 원응답 대조본이다. CFTC는 파일럿 기간에 해당하는 2024·2025년 ZIP만 남겼다.
- `backfill.py`를 `02_backfill_10y.py`로 이름만 변경했다. 2016~2025년 Parquet 12개, `.env`, 레거시 CSV 8개, 학부 notebook 4개·보고서 5개, 기존 테스트·requirements를 보존했다.
- 파일럿 notebook은 10셀, 출력은 검증 표 2개·가격 그래프 1개다. 기본 실행은 로컬 원응답을 읽으며 `REFRESH=True`일 때만 백필 스크립트로 작은 구간을 수집한다. manifest·run_id 폴더를 만들지 않는다.
- 검증: 기존 `.venv` 새 커널에서 전체 실행 통과. 12개 데이터셋의 날짜·행 수·공통 관측값을 이전 검증 CSV와 대조했다. COT는 날짜순으로 맞춰 비교했고, Yahoo 원응답의 Volume 결측을 유지했다. 커피 OHLC 범위 이탈은 기존과 같은 73행이다.
- 기본 샌드박스의 loopback 제한 때문에 notebook 실행은 로컬 소켓 권한으로 재실행했다. 외부 API 재수집·학습·패키지 변경·commit·push는 하지 않았다.
- 다음: 과거 Close 피처로 CatBoost와 단순 기준 모델 비교 → FastAPI·차트 화면.
