# 데이터를 모으고 분석하며 겪은 문제

## 기상 원자료는 있는데 rolling 결과가 비었다

처음에는 30일 기상 집계의 빈칸을 중앙값으로 채웠다. 원자료를 다시 보니 결측이 아니라 계산 시작점의 문제였다. 1월 1일부터 잘라서 30일 이동평균을 구하면 첫 29일은 관측 수가 모자란다.

![기존 Jupyter에서 확인한 기상 30일 집계의 버퍼 전후 비교](images/weather_warmup_output.png)

위 이미지는 이전 2016년 우일라 분석의 실제 출력이다. 새 03의 출력과 혼동하지 않도록 남긴다. 지금은 2014년 7월부터 자료를 받아 2015년 이전을 계산 버퍼로 쓴다. 30·90일 집계와 이전 가뭄을 계산한 뒤 확인하니 여섯 지점 모두 첫 학습일과 Train의 기상 피처 결측이 0개였다. 평균·중앙값 대치는 하지 않았다.

월별 Train 평균을 빼는 것은 이상기후 편차의 기준을 잡는 일이다. 빈 관측값을 평균으로 채우는 일과는 다르다. 미래 쪽 버퍼도 저장되어 있지만 과거 피처 계산에는 들어가지 않는다.

## 가격 누락을 먼저 지우면 거래일 간격이 달라진다

새 백필의 거래일 달력에는 2014-09-18, 2016-10-10, 2016-11-11, 2018-12-05의 가격이 비어 있다. 앞의 한 날짜는 버퍼, 나머지 세 날짜는 Train에 해당한다. 반대로 Yahoo에는 휴장일의 행도 있다.

커피 거래일을 먼저 정렬하고 빈 칸을 남긴 채 `shift(-h)`를 적용했다. 가격이 필요한 입력·정답을 만들 수 없는 사례만 마지막에 제외한다. 빈 행을 먼저 삭제하면 5거래일 뒤라고 만든 정답이 실제로는 더 먼 날짜가 될 수 있다. 합성 가격에 결측 한 칸을 넣어 이 차이를 검사했다.

## 종가가 고가·저가 밖으로 나갔다

파일럿에서 Yahoo 원응답·yfinance·저장값을 대조했을 때 같은 불일치가 있었다. 이번 2014년 7월~2025년 백필에도 커피 종가의 OHLC 범위 이탈이 455행 있었다. 수집 과정에서 잘라내거나 보간하지 않았다.

선물 정산가와 마지막 체결가는 다를 수 있다. 다만 Yahoo의 일별 계약 연결 규칙과 계약별 공식 정산가를 대조하지 못해 정산이나 롤오버를 확정 원인으로 쓰지는 않는다. 종가 위주의 피처를 사용하고, 로그수익률에도 계약 연결 영향이 남을 수 있음을 적었다. 이전 파일럿 결과는 [01 notebook](../data_code/01_small_batch_probe.ipynb)에 남아 있다.

## 관측일이 곧 공개일은 아니었다

FRED 현재값에 며칠을 더하는 방식으로는 당시 수정 전 값을 복원할 수 없다. 금리·BRL 환율·WTI는 ALFRED의 `output_type=4`로 최초 공개값과 공개 날짜를 함께 저장하도록 바꿨다. 공개 시각까지는 없어서 다음 날부터 쓴다. 처음 공개된 값을 유지하는 정책이며 이후의 모든 수정본을 반영하는 정책은 아니다.

DFF의 모든 과거 공개본을 한 번에 요청하자 API가 2,000개 공개본 제한으로 400을 반환했다. 관측 연도별로 요청하고 다음 해 상반기까지의 공개 기간을 포함해 해결했다. 응답의 관측일·공개 날짜·중복·범위를 확인했고 키가 포함된 URL은 출력하지 않았다.

달러지수는 기간만 맞춰서 쓸 수 없었다. `DTWEXBGS`는 2019년 도입 당시 과거 값을 소급 제공한 계열이라 2015년 당시 알려진 지수로 사용할 수 없다. COT도 셧다운·발표 지연이 있어 화요일+3일로 일괄 처리할 수 없다. 두 자료는 보관하고 이번 기본 입력에서는 제외했다.

Yahoo·NASA의 수정 전 자료는 확보하지 못했다. 사용자가 승인한 현재 제공 자료 기반 연구용 비교로 진행하며, ALFRED 공개본과 같은 수준의 과거 시점 복원을 했다고 쓰지 않는다.

## 가뭄×서리를 만들었지만 값이 전부 0이었다

브라질 세 지점의 격자 최저기온으로 0°C 미만 강도를 계산했다. Train에서 서리와 가뭄×서리 여섯 피처가 모두 상수였다. 모델에서 제외했고, 실제 서리 효과를 검증했다고 해석하지 않았다.

격자 평균의 저온은 농장 단위 서리 관측과 다르다. 임계값을 높여 5°C나 10°C를 서리로 부르는 식으로 신호를 만들지는 않았다. 이번 기후 그룹 비교는 강수·기온 편차, 강수 결손과 월 주기 변수의 효과를 보는 실험이다.

## 중복이 줄었다고 예측 오차도 줄지는 않는다

이전 실험에서는 VIF를 낮춰도 CatBoost 오차가 개선되지 않았다. 이번에는 Train의 기상 상관이 0.90 이상인 쌍에서 타깃 관계가 약한 쪽을 제외한 후보를 만들고, 원본 구성과 같은 날짜·CatBoost 설정으로 비교하도록 구성했다. 실제 월별 편차 입력에서는 최대 상관이 약 0.886이라 제거 기준에 해당하는 쌍이 없었다. 같은 피처의 순서만 바꾼 구성을 축소 실험으로 세지 않고 생략했다.

가격에 기후·거시를 각각 더하고 함께 넣는 그룹 비교도 추가했다. 한 변수의 단독 실패로 나머지를 버리지 않는다. CCF의 최고 상관 시차도 바로 피처로 채택하지 않는다. 입력 시차와 예측 기간을 나누고, 큰 상관이 실제 Validation 오차 감소로 이어지는지 따로 확인한다.

## 통계 모델의 업데이트도 확인해야 했다

이전 sktime 지수평활 경로에서는 `update_params=False`가 새 관측을 예상대로 반영하지 않는 경우가 있었다. 새 03은 statsmodels 상태공간 결과에 관측을 `append(refit=False)`하고 PyCaret에는 이 모델을 연결한다. 가격의 빈 칸은 관측 갱신을 건너뛰며 날짜를 유지한다.

평가 루프는 분위수가 아닌 점예측을 요청하고 예측 인덱스가 정확히 `t+h`인지 검사한다. 같은 방식을 Validation과 Test에 적용한다. 결과가 Naive와 비슷하다는 사실만으로 시장 효율성이나 과적합이 없음을 증명했다고 쓰지 않는다.

재학습 구간에서 ARIMA 기본 최적화의 수렴 실패도 확인했다. 로그가격의 작은 분산은 집중 우도로 처리하고, Powell 최적화를 Validation·Test 양쪽에 동일하게 적용했다. 수렴하지 않은 결과를 그대로 비교표에 넣지는 않았다.

## 앙상블을 만들고 나서 비교 기준을 다시 봤다

[03-2 notebook](../data_code/03_2_horizon_model_validation.ipynb)에서는 Validation 방향 정확도가 50% 이상인 모델·피처 조합 중 RMSE가 낮은 두 개를 골라 로그수익률을 반씩 평균했다. 선택은 2022~2023년에서 끝냈고, 2024~2025년 Test에서 조합을 바꾸지 않았다. 그래도 지표 하나만 보면 결과를 과하게 해석하기 쉬웠다.

### 방향 정확도 50%를 넘었다고 충분하지는 않았다

60거래일 뒤 수익률이 양수인 비율은 Validation에서 41.04%, Test에서 79.01%였다. 같은 평가 날짜에서 항상 상승을 예측하면 79.01%를 맞히는데, 앙상블은 62.30%였다. 60일 누적 수익률의 비율이지, 하루 수익률이 양수인 날의 비율은 아니다.

50% 조건은 후보를 거르는 규칙일 뿐 통계 검정이 아니었다. 이 결과에서는 항상 상승이라는 단순 방향 기준을 넘지 못했다. 다만 모델이 맞힌 사례가 모두 우연이었다고 결론 내릴 수도 없다. 상승·하락을 각각 얼마나 맞혔는지는 전체 정확도만으로 알 수 없어서, 다음에는 혼동행렬과 클래스별 재현율·Balanced Accuracy도 확인할 필요가 있다. 이번 기록에서 새로 계산한 지표는 아니다. [Balanced Accuracy 설명](https://scikit-learn.org/stable/modules/model_evaluation.html#balanced-accuracy-score)

항상 상승·항상 하락을 함께 비교하되, Test를 본 뒤 다수 방향을 골라 배포 규칙으로 삼지는 않는다. 특히 60일 수익률은 이웃한 행끼리 가격 변동을 많이 공유한다. 443행을 독립적인 방향 예측 443회로 보고 유의성을 판단하면 안 된다. 이 지평에서는 방향 필터 없이 RMSE로만 골라도 같은 두 모델이 선택됐으므로, 앙상블의 변화가 50% 필터 덕분이라고 설명하지 않는다.

시장 배경으로는 FAO가 2024년 커피 가격 상승에 베트남·인도네시아의 공급 감소와 브라질의 악천후가 작용했다고 설명한다. 이 자료가 이번 모델의 오차 원인을 검증해 주는 것은 아니다. 베트남 가뭄이나 홍해 물류 문제가 62.30%라는 결과를 만들었다는 인과관계는 이번 분석에서 확인하지 않았다. [FAO 시장 설명, 2025-03-14](https://www.fao.org/north-america/news/details/adverse-climatic-conditions-drive-coffee-prices-to-highest-level-in-years/en)

### 검증에서 잘 맞던 조합이 Test에서는 단독 모델보다 나빠졌다

60일의 가격+거시 DLinear와 전체 피처 LightGBM을 비교했다. 아래 RMSE는 모두 로그수익률 단위다.

| 항목 | Validation | Test |
|---|---:|---:|
| 두 모델의 오차 상관계수 | 0.74947 | 0.90429 |
| DLinear RMSE | 0.14719 | 0.16794 |
| LightGBM RMSE | 0.15041 | 0.18317 |
| 50:50 앙상블 RMSE | 0.13872 | 0.17236 |
| DLinear 대비 RMSE 개선율 | +5.76% | -2.63% |

Test에서 두 모델의 오차는 더 비슷하게 움직였고, 평균을 내도 DLinear 단독보다 좋아지지 않았다. 하지만 0.90429는 공분산이 아니라 단위 없는 상관계수다. 공분산은 상관계수에 두 오차의 표준편차를 곱한 값이다. 0.9를 넘으면 앙상블 효과가 사라진다는 임계값도 없다.

RMSE를 설명할 때는 오차 분산뿐 아니라 평균 오차도 봐야 한다. 같은 날짜의 오차를 `e1`, `e2`라고 두면 50:50 평균의 관계는 다음과 같다.

```text
MSE(앙상블) = 0.25 × MSE(모델1) + 0.25 × MSE(모델2)
              + 0.5 × mean(e1 × e2)
MSE = 오차 분산 + (평균 오차)²
```

여기서 분산은 같은 평가 행에 대해 분모를 n으로 계산한다. 두 모델의 오차 크기·평균·함께 틀리는 정도가 모두 영향을 준다. 평균적인 구성원 성능보다 좋아도 가장 좋은 단독 모델보다 나쁠 수 있다. 시장 국면 변화가 원인이라는 설명은 가능한 가설이지만, 두 기간의 상관계수만으로 원인을 확정하지 않는다. 학습 자료도 Train에서 Dev로 늘려 재학습한 결과다.

그래서 앙상블은 Naive뿐 아니라 선택한 단독 모델과도 비교한다. 현재 결과에서는 단독 모델을 자동 교체하지 않았다. 이후 조합이나 가중치를 탐색할 때도 시간 구간을 옮겨 보고, 상관계수가 낮다는 이유만으로 짝을 고르지는 않는다.

### 수익률 개선율을 가격 단위로 다시 확인했다

20일 ARIMA·CatBoost 앙상블의 Test 483행을 같은 날짜의 Naive와 비교했다.

| 지표 | Naive | 앙상블 | 개선율 |
|---|---:|---:|---:|
| 로그수익률 RMSE | 0.11300 | 0.11209 | 0.81% |
| 가격 RMSE (센트/파운드) | 36.09749 | 36.02569 | 0.20% |
| 가격 MAE (센트/파운드) | 26.46180 | 25.94981 | 1.93% |

가격은 각 예측일의 `P[t] × exp(예측 로그수익률)`로 환산해 오차를 다시 계산한다. 따라서 로그수익률 RMSE의 0.81% 개선을 가격 RMSE의 개선율로 그대로 옮길 수 없다. 가격 RMSE 차이는 약 0.0718센트/파운드였다. 매일 오차가 그만큼 줄었다거나 그만큼의 수익을 얻었다는 뜻은 아니다. MAE를 보면 차이는 약 0.5120센트/파운드여서 지표에 따라 개선폭도 달랐다. [MAE·RMSE의 단위와 해석](https://otexts.com/fpp3/accuracy.html)

현재 개선폭만으로 복수 모델 서빙을 채택하기에는 근거가 부족하다고 봤다. 다만 추론 시간·메모리·운영 비용을 측정하지 않았으므로 비용 대비 효과가 나쁘다고 확정하지는 않는다. 모델이 두 개라고 수집과 공통 피처 계산까지 이중화해야 하는 것도 아니다. 20일의 연도별 로그수익률 RMSE 개선은 2024년 +3.33%, 2025년 -0.85%로 달라, 작은 평균 개선의 지속성부터 확인할 필요가 있다.

기존 단독·앙상블 결과는 보존한다. 현재 Yahoo·NASA 수정 자료, 이미 여러 번 확인한 Test, 단일 seed라는 조건도 그대로다.
